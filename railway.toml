[build]
builder = "nixpacks"
buildCommand = "cd lease-scraper && npm install && npm run db:generate && npm run build"

[deploy]
startCommand = "cd lease-scraper && npm run api"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
